services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DVC_DATA_DIR: /data
    ports:
      - "8001:8001"
    volumes:
      - duo_data:/data
    restart: unless-stopped

volumes:
  duo_data:
